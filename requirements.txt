# Core numerical utilities
numpy>=1.24
scikit-learn>=1.3

# Model backends for dense retrieval, reranking, LLM baseline, and CLIP baseline
torch>=2.1
torchvision>=0.16
transformers>=4.40
sentence-transformers>=2.6
peft>=0.17

# Image loading for raw image retrieval
Pillow>=10.0
